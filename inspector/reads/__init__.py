"""Read projections (Phase 2a) — each projects PUBLISHED snapshot material for one Operation
Identity. Read-only: no traversal, no evaluation, no derived relationships (that is Phase 2b).

Signature: `(snapshot: Snapshot, params: dict) -> (status, payload)`.
"""
